export interface IColors {
  background: string
  textDark: string
  white: string
  danger: {
    light: string
    main: string
    dark: string
  }

  gray: {
    light: string
    main: string
    dark: string
  }

  primary: {
    lighter: string
    light: string
    main: string
    dark: string
  }
}
